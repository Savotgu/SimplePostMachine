#ifndef POSTLOGIC_H
#define POSTLOGIC_H

#include <iostream>
#include <string>
#include <stdlib.h>

class PostLogic{
public:
    int sum(std::string &text);
    int sub(std::string &text);
};

#endif // POSTLOGIC_H

